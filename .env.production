# Production environment configuration for TSA Platform

# Amplify environment name
AMPLIFY_ENV=prod

# Deployment options
AMPLIFY_DEPLOYMENT_BUCKET=tsa-platform-deployments-prod
AMPLIFY_DIFF_DEPLOY=true

# Function configuration
AMPLIFY_FUNCTION_TIMEOUT=30
AMPLIFY_FUNCTION_MEMORY=512

# GraphQL API endpoint will be auto-configured in production